const mealPrice = 15.0;
const tipPercentage = 0.1; // 10% tip
const tipAmount = mealPrice * tipPercentage;
const totalCost = mealPrice + tipAmount;
console.log("The total cost of your bill is: " + totalCost + " euros.");
